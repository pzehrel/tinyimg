---
phase: 04-fallback-and-concurrency
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tinyimg-core/src/compress/__tests__/web-compressor.test.ts
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "TinyPngWebCompressor tests pass completely"
  artifacts:
    - path: "packages/tinyimg-core/src/compress/__tests__/web-compressor.test.ts"
      provides: "Test coverage for web compressor"
      contains: "6 passing tests for upload, download, retry, error handling"
  key_links:
    - from: "web-compressor.test.ts"
      to: "FormData.pipe()"
      via: "mock request with stream interface"
      pattern: "mockReq.*removeListener|mockReq.*pipe"
---

<objective>
Fix FormData.pipe() mocking in web compressor tests to properly implement Node.js stream interface.

Purpose: The current mock request objects don't implement the full Node.js stream interface, causing FormData.pipe() to fail with "dest.removeListener is not a function". This is a test infrastructure issue only - the implementation code is correct.

Output: 6 passing tests for TinyPngWebCompressor with proper stream mocking
</objective>

<execution_context>
@/Users/pzehrel/Documents/repositories/opensource/tinyimg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pzehrel/Documents/repositories/opensource/tinyimg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-fallback-and-concurrency/04-VERIFICATION.md
@.planning/phases/04-fallback-and-concurrency/04-02-SUMMARY.md
@.planning/phases/04-fallback-and-concurrency/04-CONTEXT.md
@packages/tinyimg-core/src/compress/web-compressor.ts
@packages/tinyimg-core/src/compress/__tests__/fixtures.ts
@packages/tinyimg-core/src/compress/__tests__/web-compressor.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mock request stream interface in web-compressor tests</name>
  <files>packages/tinyimg-core/src/compress/__tests__/web-compressor.test.ts</files>
  <read_first>
  1. packages/tinyimg-core/src/compress/__tests__/web-compressor.test.ts (current test file with failing mocks)
  2. packages/tinyimg-core/src/compress/web-compressor.ts (implementation to understand stream usage)
  3. packages/tinyimg-core/src/compress/__tests__/fixtures.ts (existing mock patterns)
  </read_first>
  <action>
  Fix the mock request objects to properly implement the Node.js stream interface required by FormData.pipe().

  **Problem**: The current mock request objects (lines 53-59, 113-119, 158-167, 187-192, 238-243, 310-319) are missing the `removeListener` method and other stream methods that FormData.pipe() expects.

  **Required changes**:

  1. For ALL mock request objects, add the complete stream interface:
     - `removeListener: vi.fn()` - Required by FormData.pipe()
     - `eventNames: vi.fn().returns([])` - Standard stream method
     - `setMaxListeners: vi.fn()` - Standard stream method
     - `destroy: vi.fn()` - Standard stream method

  2. Update the mock request pattern to include all methods. Example:
     ```typescript
     const mockReq = {
       emit: vi.fn(),
       on: vi.fn(),
       removeListener: vi.fn(),      // ADD THIS - required by pipe()
       eventNames: vi.fn().returns([]),  // ADD THIS
       setMaxListeners: vi.fn(),      // ADD THIS
       destroy: vi.fn(),              // ADD THIS
       write: vi.fn(),
       end: vi.fn(),
     }
     ```

  3. Apply this pattern to ALL 6 mock request locations:
     - Line 53-59: First test (upload and download)
     - Line 113-119: Second test (multipart headers)
     - Line 158-167: Third test (retry on error - failed case)
     - Line 187-192: Third test (retry on error - success case)
     - Line 238-243: Fourth test (parse compressed image)
     - Line 287-293: Fifth test (HTTP errors)
     - Line 310-319: Sixth test (maxRetries)

  4. Keep all existing test logic intact - only add the missing stream methods.

  **Reference**: Node.js Stream interface requires `on`, `removeListener`, `emit`, `eventNames`, `setMaxListeners`, `destroy` methods for Writable streams. FormData.pipe() uses `removeListener` to clean up event listeners after piping completes.
  </action>
  <verify>
  <automated>cd /Users/pzehrel/Documents/repositories/opensource/tinyimg/packages/tinyimg-core && npx vitest run compress/__tests__/web-compressor.test.ts --reporter=verbose</automated>
  </verify>
  <done>
  - All 6 web compressor tests pass
  - No "dest.removeListener is not a function" errors
  - Mock request objects include complete stream interface (removeListener, eventNames, setMaxListeners, destroy)
  </done>
</task>

</tasks>

<verification>
After completion, run the web compressor test suite and verify all 6 tests pass without stream-related errors.
</verification>

<success_criteria>
- [ ] All 6 web compressor tests pass
- [ ] FormData.pipe() mocking works correctly
- [ ] No stream interface errors in test output
- [ ] Tests verify upload, download, retry, error handling, and maxRetries behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-fallback-and-concurrency/04-05-SUMMARY.md`
</output>
