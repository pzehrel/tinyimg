---
phase: 04-fallback-and-concurrency
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/tinyimg-core/src/compress/__tests__/retry.test.ts
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "RetryManager tests pass completely"
  artifacts:
    - path: "packages/tinyimg-core/src/compress/__tests__/retry.test.ts"
      provides: "Test coverage for retry logic"
      contains: "8 passing tests for exponential backoff, error handling, failure tracking"
  key_links:
    - from: "retry.test.ts"
      to: "retry.ts"
      via: "RetryManager.execute() tests"
      pattern: "RetryManager|shouldRetry|exponential backoff"
---

<objective>
Implement complete test coverage for RetryManager with exponential backoff, error classification, and failure tracking.

Purpose: The RetryManager tests are currently marked as `test.skip()` - a regression from previous verification where they were passing. This plan implements all 8 tests to verify retry logic behavior.

Output: 8 passing tests for RetryManager covering success, retries, exponential backoff, error classification, and failure tracking
</objective>

<execution_context>
@/Users/pzehrel/Documents/repositories/opensource/tinyimg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pzehrel/Documents/repositories/opensource/tinyimg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-fallback-and-concurrency/04-VERIFICATION.md
@.planning/phases/04-fallback-and-concurrency/04-01-SUMMARY.md
@.planning/phases/04-fallback-and-concurrency/04-CONTEXT.md
@packages/tinyimg-core/src/compress/retry.ts
@packages/tinyimg-core/src/compress/__tests__/retry.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RetryManager tests (TDD)</name>
  <files>packages/tinyimg-core/src/compress/__tests__/retry.test.ts</files>
  <read_first>
  1. packages/tinyimg-core/src/compress/__tests__/retry.test.ts (current test file with skipped tests)
  2. packages/tinyimg-core/src/compress/retry.ts (implementation to understand behavior)
  3. packages/tinyimg-core/src/utils/logger.ts (for logWarning behavior)
  </read_first>
  <behavior>
    Test 1: "should return result on first success"
    - Given: An operation that succeeds immediately (returns 'success')
    - When: RetryManager.execute() is called with the operation
    - Then: Returns 'success' without any retries
    - And: failureCount is 0

    Test 2: "should retry on network errors (ECONNRESET, ETIMEDOUT)"
    - Given: An operation that fails with { code: 'ECONNRESET' } twice, then succeeds
    - When: RetryManager.execute() is called with maxRetries=3
    - Then: Retries 2 times (attempts 1 and 2 fail, attempt 3 succeeds)
    - And: Returns success result

    Test 3: "should retry on HTTP 5xx errors"
    - Given: An operation that fails with { statusCode: 503 } twice, then succeeds
    - When: RetryManager.execute() is called with maxRetries=3
    - Then: Retries 2 times
    - And: Returns success result

    Test 4: "should not retry on 4xx client errors"
    - Given: An operation that fails with { statusCode: 401 }
    - When: RetryManager.execute() is called with maxRetries=3
    - Then: Fails immediately without retry (throws on first attempt)
    - And: Only 1 attempt was made

    Test 5: "should use exponential backoff delays"
    - Given: An operation that fails 3 times, then succeeds (maxRetries=3)
    - When: RetryManager.execute() is called with vi.useFakeTimers()
    - Then: Uses delays: 1000ms (2^0 * 1000), 2000ms (2^1 * 1000), 4000ms (2^2 * 1000)
    - And: Total delay = 7000ms before final attempt
    - Implementation: Use vi.advanceTimersByTime() to advance timers and verify delays

    Test 6: "should reset failure count on success"
    - Given: An operation that fails twice, then succeeds
    - When: RetryManager.execute() is called twice
    - Then: First call has failureCount=2 before success, 0 after
    - And: Second call starts with failureCount=0 (not cumulative from previous)

    Test 7: "should throw error after max retries"
    - Given: An operation that always fails with retryable error
    - When: RetryManager.execute() is called with maxRetries=3
    - Then: Attempts 4 times total (0, 1, 2, 3 = maxRetries)
    - And: Throws the error after attempt 3 fails

    Test 8: "should track failure count accurately"
    - Given: An operation that fails twice, then succeeds
    - When: RetryManager.execute() is called
    - Then: getFailureCount() returns 0 before any attempts
    - And: getFailureCount() returns 2 after two failures
    - And: getFailureCount() returns 0 after success
  </behavior>
  <action>
  Replace all `it.skip()` calls with `it()` and implement each test following the TDD workflow:

  **Step 1 - RED**: For each test, write the test implementation that verifies the specified behavior. Use vi.useFakeTimers() and vi.advanceTimersByTime() for testing exponential backoff delays.

  **Step 2 - VERIFY RED**: Run the test to confirm it fails (tests should exist but implementation already exists, so some may pass immediately)

  **Step 3 - GREEN**: The RetryManager implementation already exists in retry.ts. If tests fail, verify the implementation is correct - the tests should pass with the existing implementation.

  **Step 4 - REFACTOR**: If needed, clean up test code for clarity.

  **Specific implementation notes**:
  - Import RetryManager: `import { RetryManager } from '../retry'`
  - Use vi.useFakeTimers() in beforeEach, vi.restoreAllMocks() in afterEach
  - For exponential backoff test: use `await vi.advanceTimersByTimeAsync(delay)` to advance timers between retries
  - Create errors with codes: `const error = new Error('msg'); (error as any).code = 'ECONNRESET'`
  - Create errors with status codes: `const error = new Error('msg'); (error as any).statusCode = 503`

  **Test template for exponential backoff**:
  ```typescript
  it('should use exponential backoff delays', async () => {
    let attemptCount = 0
    const operation = async () => {
      attemptCount++
      if (attemptCount < 4) {
        const error = new Error('Failed')
        ;(error as any).code = 'ECONNRESET'
        throw error
      }
      return 'success'
    }

    const manager = new RetryManager(3, 1000) // maxRetries=3, baseDelay=1000ms
    const promise = manager.execute(operation)

    // Advance past first retry (1000ms)
    await vi.advanceTimersByTimeAsync(1000)
    // Advance past second retry (2000ms)
    await vi.advanceTimersByTimeAsync(2000)
    // Advance past third retry (4000ms)
    await vi.advanceTimersByTimeAsync(4000)

    const result = await promise
    expect(result).toBe('success')
    expect(attemptCount).toBe(4)
  })
  ```
  </action>
  <verify>
  <automated>cd /Users/pzehrel/Documents/repositories/opensource/tinyimg/packages/tinyimg-core && npx vitest run compress/__tests__/retry.test.ts --reporter=verbose</automated>
  </verify>
  <done>
  - All 8 RetryManager tests pass (not skipped)
  - Tests verify: immediate success, network error retries, HTTP 5xx retries, 4xx no-retry, exponential backoff, failure count reset, max retries, accurate failure tracking
  - No `it.skip()` markers remain in the file
  - Tests use fake timers for delay verification
  </done>
</task>

</tasks>

<verification>
After completion, run the retry test suite and verify all 8 tests pass without any skipped tests.
</verification>

<success_criteria>
- [ ] All 8 RetryManager tests pass
- [ ] No `it.skip()` markers remain in retry.test.ts
- [ ] Tests cover exponential backoff with fake timers
- [ ] Tests verify retry behavior for network errors (ECONNRESET, ETIMEDOUT, ENOTFOUND)
- [ ] Tests verify no-retry for 4xx errors
- [ ] Tests verify retry for 5xx errors
- [ ] Tests verify failure count tracking and reset
</success_criteria>

<output>
After completion, create `.planning/phases/04-fallback-and-concurrency/04-06-SUMMARY.md`
</output>
