# Strict workspace dependency resolution
# Fail publishing if any workspace:* protocol remains unresolved
workspace-dependency-behavior=strict

# Enable provenance for npm packages (matches CI workflow)
provenance=true
