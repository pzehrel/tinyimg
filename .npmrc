# Block all publish operations unless CI environment variable is set
# This prevents accidental publishes from developer machines
# CI workflow will override this with NPM_CONFIG_REGISTRY env var
registry=http://localhost:4873/
